---
slug: next-js
name: Next.js 15 (App Router)
category: frontend
order: 1
projectSlugs:
  - timeline-portfolio
  - use-scroll-timeline
---

Server Components by default, ISR for a fast public read path, and route handlers
for the write paths. This portfolio itself is a Next.js 15 App Router app —
statically prerendered pages that regenerate on demand when content changes.

{/* @placeholder — unreviewed corpus. Review this copy, then delete this line before the Phase 3 indexer embeds it (pnpm corpus:check). */}
