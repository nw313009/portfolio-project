---
slug: react
name: React 19
category: frontend
order: 3
projectSlugs:
  - timeline-portfolio
  - use-scroll-timeline
---

Composable components, disciplined effects, and performance-minded rendering —
keeping hot paths like pointer/scroll handling off the React render cycle so
animations stay smooth.

{/* @placeholder — unreviewed corpus. Review this copy, then delete this line before the Phase 3 indexer embeds it (pnpm corpus:check). */}
