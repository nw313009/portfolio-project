---
slug: auth
name: Auth & Authorization
category: backend
order: 2
projectSlugs:
  - timeline-portfolio
---

Auth.js v5 GitHub OAuth for authentication, kept strictly separate from
authorization: an allowlist re-checked at every privileged resource, never
trusting middleware as the security boundary. authN is not authZ.

{/* @placeholder — unreviewed corpus. Review this copy, then delete this line before the Phase 3 indexer embeds it (pnpm corpus:check). */}
