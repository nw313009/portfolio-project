---
slug: typescript
name: TypeScript (strict)
category: frontend
order: 2
projectSlugs:
  - timeline-portfolio
  - use-scroll-timeline
  - devlog-cli
---

Strict mode, discriminated unions as the source of truth, and Zod schemas that
validate at every trust boundary. Types describe the domain; the UI is derived
from them rather than hardcoded.

{/* @placeholder — unreviewed corpus. Review this copy, then delete this line before the Phase 3 indexer embeds it (pnpm corpus:check). */}
