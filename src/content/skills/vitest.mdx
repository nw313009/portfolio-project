---
slug: vitest
name: Vitest (unit & integration)
category: testing
order: 1
projectSlugs:
  - timeline-portfolio
---

Fast unit tests plus real integration tests that run against a disposable Neon
branch — not mocks — so the data layer is proven, not assumed. A green test gate
after every slice is a hard rule, not a nicety.

{/* @placeholder — unreviewed corpus. Review this copy, then delete this line before the Phase 3 indexer embeds it (pnpm corpus:check). */}
