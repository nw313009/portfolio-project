---
slug: postgres-drizzle
name: Postgres & Drizzle ORM
category: backend
order: 1
projectSlugs:
  - timeline-portfolio
---

Neon Postgres with Drizzle ORM: a typed schema, forward-only migrations, and a
driver split — HTTP for fast ISR reads, a pooled WebSocket driver for
transactional writes. Append-only events, audit logging, and archival-friendly
indexing.

{/* @placeholder — unreviewed corpus. Review this copy, then delete this line before the Phase 3 indexer embeds it (pnpm corpus:check). */}
