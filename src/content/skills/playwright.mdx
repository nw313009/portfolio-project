---
slug: playwright
name: Playwright (E2E)
category: testing
order: 2
projectSlugs:
  - timeline-portfolio
---

End-to-end acceptance tests in a real browser: scroll-driven animation,
deep-link scrolling, reduced-motion behavior, and beacon round-trips — each phase
gated on a passing E2E run before it ships.

{/* @placeholder — unreviewed corpus. Review this copy, then delete this line before the Phase 3 indexer embeds it (pnpm corpus:check). */}
