---
id: devlog-cli
title: devlog
slug: devlog-cli
startDate: "2022-02-01"
endDate: "2022-06-01"
stack:
  - Node.js
  - Ink
languages:
  - TypeScript
summary: A terminal journaling tool that turns quick notes into a searchable, git-backed developer log.
githubUrl: https://github.com/example/devlog
preview:
  previewType: cli
  castUrl: https://asciinema.org/a/861939
---

`devlog` is a keyboard-first CLI for capturing what you worked on. Entries are
stored as plain Markdown in a git repo, so history, blame, and grep all just
work.

> Sample project used to exercise the `cli` preview type (no video, so it
> renders the cast-link fallback). Replace with a real recording of `devlog`
> before publishing.
