---
id: use-scroll-timeline
title: use-scroll-timeline
slug: use-scroll-timeline
startDate: "2023-01-01"
endDate: "2023-05-01"
stack:
  - React
languages:
  - TypeScript
summary: A tiny, dependency-free React hook that maps scroll progress through an element to a normalized 0-1 value.
githubUrl: https://github.com/example/use-scroll-timeline
preview:
  previewType: library
  language: tsx
  codeSnippet: |
    import { useRef } from "react";
    import { useScrollTimeline } from "use-scroll-timeline";

    export function Section() {
      const ref = useRef<HTMLDivElement>(null);
      const { progress } = useScrollTimeline(ref);
      return <div ref={ref} style={{ opacity: progress }} />;
    }
---

`use-scroll-timeline` wraps an `IntersectionObserver` and a scroll listener into
a single hook that reports how far the viewport has travelled through a target
element. It powers the line-draw animation on this very timeline.

> Sample project used to exercise the `library` preview type.
