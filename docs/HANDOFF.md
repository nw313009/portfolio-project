# HANDOFF — read this first to resume with full context

**Purpose of this file:** let a new assistant chat pick up exactly where the last one left off, with the reasoning intact — not just *what* we're building but *why* each decision was made. Read this, then `docs/ARCHITECTURE.md`, `docs/phases/PHASES.md`, and `docs/PROGRESS.md` (the live state).

## Who I am / working style
Solo developer, building a portfolio site, implementing via Cursor AI agents. New to the external tooling (Neon, Vercel, Upstash, OAuth) — the assistant should give exact click-by-click steps for any external tool and never assume prior familiarity. I work phase-by-phase, plan-then-implement, and want the assistant to technically architect and direct (elite system-level thinking) while Cursor does the implementation. Move fast, but gate every stage with tests. I use Cursor Ultra (cost is not a constraint on model choice).

## What we're building (the goal, from the start)
A portfolio site that (1) proves elite frontend engineering (Next.js 15 + TypeScript), (2) reads as a vertical center-line timeline — oldest project at top, scroll-driven, paginated, modern UX/UI, (3) grows a Python/FastAPI backend + AI that lets visitors interact with projects and tracks those interactions as metrics. Admin (me) can add projects via GitHub link with previews; public visitors need no login. End goal includes hover previews of projects and an admin/public split.

## Decisions already locked (and the reasoning)
- **Ship order:** beautiful frontend first (Vercel), backend/AI later — a modular monolith, NOT premature microservices. The Python/FastAPI service is introduced only when AI gives it a real job (Phase 3). Reasoning: a solo dev shipping microservices early spends all their time on infra instead of shipping.
- **Preview strategy:** live **iframe of my deployed demo** for web apps; recorded media / snippets for other project types. Projects span MANY languages, so preview is a **typed discriminated union** on `previewType` (webapp|cli|service|library|notebook|media) — a Rust CLI can't be iframed like a Next.js app. Hard invariant: **never execute untrusted repo code on our infra** (that's building your own Vercel — security/ops sinkhole). Evaluated DIY sandboxed builds vs managed microVMs vs browser execution and chose iframe for blast-radius + effort reasons.
- **Hosting:** Vercel now; AWS enters only with the FastAPI service (Phase 3).
- **Stack:** Next.js 15 App Router + TS strict + Tailwind + shadcn/ui + framer-motion; Neon Postgres + Drizzle; Auth.js v5; Playwright previews. Phase 2 also adds Vercel Blob (media) + Upstash Redis (rate limiting) — all serverless, scale-to-zero, free-tier, no AWS.
- **Models in Cursor:** Sonnet 5 default, Opus 4.8 for architectural/reasoning-heavy slices (schema design, authZ middleware, ingestion error handling).
- **authZ boundary is the resource, not the middleware.** The middleware matcher is an optimistic UX gate and is fail-open by design (a route not in the matcher just doesn't get the pre-redirect). Security lives at the resource: **every privileged server operation (route handler / server action / admin server component) MUST call `require-admin()`** — no exceptions. This is the north-star "admin writes always re-check `isAdmin`" made concrete. Reasoning: middleware-only auth is bypassable (CVE-2025-29927); JWT flags can go stale, so authZ re-derives from the live `ADMIN_EMAILS` every call.
- **`events.type = 'hover'` MEANS "preview button clicked", NOT hover-intent.** Slice 5 reused the existing Slice 1 enum value rather than migrate; the dashboard relabels it "Preview opened". Hover-intent is deliberately NOT tracked (noisy, no touch equivalent, not intent). The raw enum value is therefore semantically wrong — rename it to `preview_open` on the NEXT `events` enum migration, whenever one happens for another reason. Do not migrate solely for this.

## How we keep Cursor on-goal (the governance system)
- `.cursor/rules/00-north-star.mdc` — always-apply: goal + hard invariants + operating protocol (work one phase at a time; Plan mode scoped to current phase; test gate after each slice; update PROGRESS; STOP at phase boundary).
- `.cursor/rules/10-frontend-conventions.mdc` — scoped to src/**.
- `docs/phases/PHASES.md` — the spec per phase: goal, in-scope, **out-of-scope guards**, definition of done, test gate, kickoff prompt, suggested model, locked infra decisions, Context7-verified patterns.
- `docs/PROGRESS.md` — live state; a fresh Cursor agent reads it first every session.
- **Rhythm:** fresh Cursor chat per slice; each slice ends with a green Test Gate (`pnpm typecheck && lint && build && test` + Playwright), PROGRESS updated, then a pause. I report the result to the assistant, who reviews it against the slice's Definition of Done before I continue.

## MCPs configured in Cursor
Context7 (current API docs — auto-invoked), Playwright (visual verification + preview capture), AWS (Phase 3 only — keep OFF until then), Figma (only for a specific design slice — keep OFF otherwise). Tool-cap discipline: past ~40 tools Cursor degrades, so only enable what the current phase needs.

## Where we are RIGHT NOW
- **Phase 1: COMPLETE** — timeline frontend built, tested, deployed to Vercel.
- **Phase 2: DEPLOYED THROUGH SLICE 5** (Slice 6 + the preview-rendering slice + the e2e hygiene commit remain).
  - **DEPLOYED TO PROD (Phase 2 complete through Slice 5).** `origin/main` @ `e67f87b`, prod URL https://portfolio-project-self-rho.vercel.app. `main` migrated + information_schema-verified BEFORE the merge (0000 + 0001; expand-only, so rollback stays safe). Verified live: timeline renders, prod GitHub login reaches /admin, dashboard, beacon, ingest.
  - **Slice 1 (data layer): COMPLETE + committed.** Neon + Drizzle; tables projects/events/audit_log; events.ts and events.(projectId,ts) indexed; events has NO FK to projects (archival-friendly). Driver split wired (neon-http reads / neon-serverless Pool transactional writes). Integration tests run for real against the Neon **test** branch. Gotcha caught: drizzle-kit's dotenv reload silently redirected the test-branch migration back to dev while reporting success — verify against information_schema directly; fix committed.
  - **Slice 2 (read path on DB): COMPLETE + accepted + LIVE IN PROD.** page.tsx is now an async Server Component reading getPublishedProjectEntries() via neon-http instead of MDX; ISR via `export const revalidate = 3600`; public read path stays static/cached, no auth. Idempotent seed (`pnpm db:seed`, onConflictDoUpdate). Presentation untouched. Gate green: typecheck/lint/build/test (69/69) + e2e (8/8). Bugs fixed: top-level await in seed under CJS; clock skew between Node `new Date()` and Postgres defaultNow() → standardized on `sql now()`. Committed. **Deployed to Vercel and verified live — the 4 projects render on the production URL (Pixel Dungeon → devlog → use-scroll-timeline → Timeline Portfolio), prerendered static.**
  - **DEPLOY GOTCHA (resolved, important):** first prod deploy failed with Postgres `parserOpenTable` because migrations/seed had only ever been applied to the `dev`/`test` branches — the `main` (prod) branch was empty. Fix: ran `drizzle-kit migrate` + `pnpm db:seed` pointed at `main`'s connection strings (unpooled for migrate, pooled for seed), verified via information_schema on `main`, unset the `$env:` overrides, confirmed `.env.local` still targets dev. This recurs on every schema change — `main` must be migrated deliberately.
  - **Two Slice 2 follow-ups: COMPLETE + committed (`05b2b45`).** (a) **Build resilience** — timeline DB read wrapped in `loadPublishedProjects()` (try/catch, logs on failure, falls back to `[]`); renders an empty-state instead of `<Timeline>` on empty-or-error. Proven against the real failure mode (unreachable host → `ENOTFOUND` caught + logged, build still completed, `/` prerendered) plus 3 unit tests (success / DB-failure / zero-rows). `revalidate = 3600` intact so ISR still backfills. (b) **Migration-on-deploy** — manual runbook in `docs/DEPLOY.md` (migrate `main` → seed `main` → verify via information_schema → deploy); automation deliberately deferred to Phase 4, reasoning logged in DEPLOY.md + PROGRESS decisions log. Gate green: 72/72 unit/integration, 8/8 e2e.
  - **Slice 3 (auth): COMPLETE + accepted + committed.** Auth.js v5 (`next-auth@5.0.0-beta.31`), Next.js 15.5.20 (≥15.2.3, CVE-2025-29927 closed). Split config: `src/auth.config.ts` (edge-safe — providers + callbacks + `trustHost`, no DB/Node imports) imported by both `src/auth.ts` (JWT strategy, no DB adapter; exports handlers/auth/signIn/signOut) and `src/middleware.ts`. GitHub provider requests `read:user user:email`; `jwt` callback resolves the primary **verified** email via GitHub `/user/emails` (private-email accounts never return null). authN/authZ separated: pure `isAdmin()` in `src/lib/auth/is-admin.ts`. **Two-layer authZ:** `middleware.ts` = optimistic gate only (positive matcher `/admin/**` + `/api/admin/**`, so `/` stays `○ Static`/ISR); `src/lib/auth/require-admin.ts` = authoritative, re-runs `auth()` + `isAdmin(ADMIN_EMAILS)` server-side **re-derived from the live allowlist, not a stale token flag**. Stubs: protected `/admin` server component + `POST /api/admin/ping`, `[...nextauth]` route, `next-auth.d.ts`. Gate green: typecheck/lint/build, `pnpm test` 92/92, `pnpm test:e2e` 11/11; authZ tests inject the principal (no live OAuth), proving authenticated-but-NOT-allowlisted → denied at both the resource layer (independent of middleware) and the optimistic gate. Gotcha fixed: `UntrustedHost` under local `next dev`/`start` (Auth.js auto-trusts host on Vercel, not locally) → `trustHost: true` in the shared edge config. **Lock-out check PASSED — verified live: real GitHub login reaches `/admin`, so `ADMIN_EMAILS` matches the resolved verified email.** NOT deployed to prod.
  - **Slice 4 (GitHub ingestion): COMPLETE + committed.** Admin-only ingest→draft→publish flow; SSRF-safe metadata fetch; atomic project+audit writes via the Pool; metadata-only render. **Now live in prod** (shipped in the Slice 5 merge).
  - **Slice 4 human-verified (retroactively confirmed during Slice 5):** the ingest→draft→publish→render flow WAS exercised manually — the ingested project "Ticket Service" is published and rendering on the dev timeline (discovered because it broke a stale exact-match Phase 1 e2e assertion). The earlier "NOT HUMAN-VERIFIED" caveat is withdrawn.
  - **Slice 5 (events + dashboard): COMPLETE + accepted + LIVE IN PROD.** Public `POST /api/events` beacon (bot filter → per-IP Upstash rate limit → strict Zod → known-project check → Pool write with server `now()`); ephemeral in-memory session id; `requireAdmin()`-gated `/admin/dashboard` of aggregate counts. `/` stays `○ Static`+ISR. No schema/enum migration. Gate green: typecheck/lint/build, `pnpm test` 179/179, `pnpm test:e2e` 12/12 (the one stale Phase 1 assertion relaxed to a subsequence — see decisions log).
- **Environment (resolved, logged in PROGRESS.md):** Node/npm/pnpm now on the persistent Windows PATH (agent runs PowerShell; user's Git Bash fixed via .bashrc). Playwright Chromium installed — add a `playwright install` postinstall guard so a clean clone can't regress. Node is v23 (non-LTS) — fine for now, consider LTS later, not mid-phase.
- **PHASE 2 EXTERNAL PROVISIONING — COMPLETE. Do NOT re-issue setup steps.** All Phase 2 external tools and env vars are already provisioned by the developer. No chat re-issues external setup or env-var creation for Phase 2; code must MATCH the already-provisioned names below, not create them.
  - GitHub OAuth app (dev) created; Client ID/secret in place. Callback URL was set to `http://localhost:3000/api/auth/callback/github` (Auth.js v5 default).
  - **TWO GitHub OAuth apps — do not conflate.** "Portfolio (dev)" → callback `http://localhost:3000/api/auth/callback/github`, its Client ID/secret live in `.env.local`. "Portfolio (prod)" → callback `https://portfolio-project-self-rho.vercel.app/api/auth/callback/github`, its Client ID/secret live in Vercel (Production scope). A GitHub OAuth App has exactly ONE callback URL field — multiple callbacks are a GitHub *Apps* feature, a different product. Deploy gotcha hit + resolved: prod initially ran the DEV client_id → `redirect_uri is not associated with this application`; fix was pointing Vercel's `AUTH_GITHUB_ID`/`AUTH_GITHUB_SECRET` at the prod app + redeploy (env changes need a new build). `trustHost: true` derives the canonical domain correctly — the `AUTH_URL` pin was NOT needed. If a custom domain is ever attached: update the prod app's callback + homepage or auth breaks.
  - `.env.local` fully populated, matches `.env.example` (names only); `.env.local` untracked.
  - Vercel mirror complete — all 10 Phase 2 vars with prod values (`DATABASE_URL` = `main` branch string; `TEST_DATABASE_URL` excluded).
  - **Exact auth var names (Auth.js v5 convention — use these verbatim):** `AUTH_SECRET`, `AUTH_GITHUB_ID`, `AUTH_GITHUB_SECRET`, `ADMIN_EMAILS`. NOT the legacy `NEXTAUTH_SECRET`/`GITHUB_ID`. `ADMIN_EMAILS` IS already provisioned (present in Vercel + `.env.local`), set by the developer to their GitHub account email — Slice 3 does NOT add it; it consumes it.
  - **LOCK-OUT RISK (developer must verify before trusting the gate):** the value of `ADMIN_EMAILS` must EXACTLY match the developer's GitHub primary/verified email, and that email must actually be returned by OAuth. GitHub returns null email for private-email accounts unless the code requests `user:email` scope and resolves the primary verified email via the GitHub `/user/emails` API. Slice 3 must request `user:email` and resolve primary email; developer must confirm the match, or they lock themselves out of their own admin.
  - ~~The ONLY remaining external action in Phase 2 is adding the PROD GitHub OAuth callback URL at deploy time.~~ **DONE** — a separate "Portfolio (prod)" OAuth app was created with the prod callback and its Client ID/secret set in Vercel (see the "TWO GitHub OAuth apps" note above). Phase 2 external actions complete.
- **Auth scaffolded (Slice 3):** `next-auth@5` installed. Files: `src/auth.config.ts`, `src/auth.ts`, `src/middleware.ts`, `src/lib/auth/is-admin.ts`, `src/lib/auth/require-admin.ts`, `src/app/api/auth/[...nextauth]/route.ts`, `src/app/admin/page.tsx`, `src/app/api/admin/ping/route.ts`, `next-auth.d.ts`. JWT sessions, no DB adapter. Deferred to deploy time: add prod OAuth callback `https://<domain>/api/auth/callback/github` to the existing GitHub app; optionally pin `AUTH_URL` to the canonical prod origin.

## Deferred backlog (not scheduled)
- **E2E runs against the mutable dev DB — real fix: seed a known state first.** Acceptance tests currently read whatever data happens to be in the dev branch, so they're coupled to leftover rows (this is exactly what broke the Phase 1 exact-match assertion when the ingested "Ticket Service" was published; fixed in Slice 5 by relaxing that one assertion to a subsequence). The durable fix is to run e2e against a freshly-seeded known state — the idempotent `pnpm db:seed` already exists — so assertions are deterministic and can be exact again. A real fix, not a slice. **File this next to the still-outstanding Playwright `postinstall` guard below.**
- **Flaky e2e: `timeline.spec.ts:112` (mobile single-rail alignment).** Observed intermittently failing under the 6-worker parallel run (a card measured mid-layout → x=120 vs 72), but passes 3/3 in isolation and green on full re-run. Not touched (out of Slice 5 scope). Likely a measurement-before-layout-settles race — stabilize later by waiting for a settled signal (e.g. `expect.poll` on the rail x, or awaiting the whileInView final position) rather than reading `getBoundingClientRect` immediately after `goto`.
- **Playwright `postinstall` guard — STILL OUTSTANDING, now two slices old.** Add a `playwright install chromium` postinstall (or equivalent) so a clean clone / fresh session can't regress on missing browsers. (Slice 5 also pinned `PLAYWRIGHT_BROWSERS_PATH=0` in the `test:e2e` script via `cross-env` so browsers persist in `node_modules` instead of the per-session sandbox temp dir — but the postinstall guard is still the clean-clone safety net.)
- Events retention/archival: move old rows to cold storage or roll up into aggregates on a schedule. Fine to defer — no traffic yet. Phase 2 kept the schema archival-friendly (indexed ts, no blocking FKs) so this stays a background job later, not a migration.
- **Authenticated GitHub API calls (rate limit).** Slice 4 ingestion calls the GitHub REST API UNAUTHENTICATED (60 req/hr/IP) — fine for admin-only use where projects are added rarely. Code already reads an optional `GITHUB_TOKEN` (5000/hr) if present, so upgrading is a one-line env-var add, no code change. **Scale trigger:** revisit the moment ingestion stops being admin-only — i.e. if the product ever lets visitors/users add their own projects, the 60/hr/IP shared limit becomes a real ceiling and a server-side token (or per-user OAuth-scoped calls) becomes required. Not needed while admin-only.

## Immediate next steps (Phase 2 remaining work)
1. **E2E hygiene commit (small; ~3 slices overdue; recommended FIRST now the deploy pressure is off):** Playwright `postinstall` guard + settled-rail poll for `timeline.spec.ts:112` + seed a known DB state for e2e (all three tracked in Deferred backlog). Not a slice — cheap, and every later slice inherits a more trustworthy gate.
2. **Slice 6 (visitor card + Resend)** — see the RATIFIED note below (needs new provisioning: Resend account + verified domain + API key).
3. **Preview-rendering slice** — normalize the two preview shapes FIRST, then build the `previewType` discriminated-union renderer, with **Vercel Blob folded into THIS slice** for the media/recorded preview types (Blob is a dependency of the media renderer, not a slice of its own).
4. **Standing deploy rule:** every future schema change → migrate `main` deliberately before deploying (runbook in docs/DEPLOY.md), until the Phase 4 pipeline job exists. If a custom domain is attached, update the prod OAuth app's callback + homepage URLs.
- **Slice 6 (optional visitor card) — RATIFIED, not yet started.** Optional "who's visiting" card: name / company / position, ALL optional, emailed to the admin, **no DB row, no schema change**. This is DECLARED identity (consent-based), NOT tracked identity — it deliberately does NOT join to the anonymous event counts (joining would require the visitor id we chose not to mint). Requires NEW provisioning: an email provider (**Resend** — serverless, free tier, fits the no-AWS Phase-2 posture); needs account + verified sending domain + API key. Threat model: an unauthenticated form that emails the admin is an email-bomb/spam vector — REQUIRES aggressive Upstash rate limiting (much tighter than the events beacon), a honeypot field, strict Zod bounds (length caps, no HTML, reject unknown fields), escaped email body, and silent failure on rate-limit. Copy must say "not tracked, not stored in our database — sent to me as an email"; do NOT claim "not stored" (it lives in the inbox). Failed send = silently lost submission (no retry/audit) — accepted trade; tell the visitor honestly on failure.

No-deviation confirmation: nothing pulls Slice 4/5 or Blob/Upstash forward, the boundary tests and STOP discipline are intact, and the three ratified extensions are additive strengthenings of the original authN/authZ-separation goal, not replacements.

## Assistant's standing instructions
- **Never paste connection strings / secrets into chat.** Copy from Neon/Vercel straight into the terminal. Incident: the prod `main` DB password leaked into a chat transcript on 2026-07-15 — **ROTATION REQUIRED, then record the date here** (Neon → `main` role → reset password → update Vercel `DATABASE_URL` + `DATABASE_URL_UNPOOLED` → redeploy). Do NOT mark this resolved until the developer confirms the rotation.

Architect and direct; keep Cursor from losing the goal or drifting past scope; give exact step-by-step for every external tool without assuming prior knowledge; review each slice against its Definition of Done; hold the Slice 3 auth pause firmly; remember the reasoning behind every locked decision above. **Do NOT re-issue Phase 2 external setup or env-var creation — it is COMPLETE (see provisioning block); code must match the provisioned names, never recreate them.** Flag any extension of a locked spec for the developer to ratify rather than sliding it in.